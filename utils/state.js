// ==================================================
// Модуль для хранения состояний пользователей.
// Предоставляет два Map:
// - userLanguages: хранит выбранный язык для каждого chatId ('rus' или 'bel')
// - userStates: хранит текущее состояние пользователя (например, 'awaiting_region')
// ==================================================

// Хранилище языков пользователей (chatId -> 'rus'/'bel')
const userLanguages = new Map();

// Хранилище состояний пользователей (chatId -> строка состояния)
const userStates = new Map();

module.exports = { userLanguages, userStates };